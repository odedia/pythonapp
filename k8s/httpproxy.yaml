apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: basic
spec:
  virtualhost:
    fqdn: pyapp.spring.tanzutime.com
  routes:
    - conditions:
      - prefix: /
      services:
        - name: pyapp
          port: 5000
      # - prefix: /
      # services:
      #   - name: frontend
      #     port: 80
